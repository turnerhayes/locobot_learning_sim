# Core dependencies (required)
pymunk>=6.5.0
gymnasium>=0.29.0
shapely>=2.0.0
numpy>=1.24.0
torch>=2.0.0
matplotlib>=3.7.0

# Rendering (required for --render flag)
pygame>=2.5.0

# Experiment tracking (optional, for --wandb flag)
wandb>=0.15.0

# Video recording (optional, auto-records eval videos after training)
imageio>=2.30.0
imageio-ffmpeg>=0.4.9
